clc;
clear;

file = "output/output";

syms x;
g = 1 / (1 + x^2);


for i=2:1:3
    figure;
    s = "N=";
    s = s + i;
    title("s");
    filename = file + i;
    data = dlmread(filename);
    [c,n] = size(data);
    d = zeros(n);
    start = data(1,1);
    a = start - 5;
    b = start + n + 5;
    [x,m] = plotTruncatedPowerFunc2Bsplines(i,start,a,b);
    m = 
    for j =1:1:n
        
        
end